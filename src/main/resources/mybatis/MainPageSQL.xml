<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nts.reservation.dao">

	<select id="selectCategoryList" resultType="CATEGORY">
		SELECT category_id AS
		id,
		category.name AS name,
		count(*) AS count
		FROM product INNER JOIN
		category
		ON category.id = product.category_id
		GROUP BY category_id
	</select>
	<select id="SELECT_PRODUCT_LIST" resultType="PRODUCT">
		SELECT
		product.content AS product_content,
		file_info.save_file_name AS
		product_image_url,
		display_info.place_name AS place_name,
		product.description AS product_description,
		product.id AS product_id,
		display_info.id AS display_info_id
		FROM product INNER JOIN display_info
		INNER JOIN product_image
		INNER JOIN file_info
		ON product.id =
		display_info.product_id
		AND product.id = product_image.product_id
		AND
		product_image.file_id = file_info.id
		AND product_image.type = 'th'
		limit #{start}, #{limit}
	</select>

	<select id="SELECT_PRODUCT_LIST_BY_CATEGORY"
		resultType="PRODUCT">
		SELECT product.content AS product_content,
		file_info.save_file_name AS product_image_url,
		display_info.place_name
		AS place_name,
		product.description AS product_description,
		product.id AS
		product_id,
		display_info.id AS display_info_id
		FROM product INNER JOIN
		display_info
		INNER JOIN product_image
		INNER JOIN file_info
		ON product.id
		= display_info.product_id
		AND product.id = product_image.product_id
		AND
		product_image.file_id = file_info.id
		AND product_image.type = 'th'
		AND
		product.category_id = #{id}
		limit #{start}, #{limit}
	</select>

	<select id="SELECT_PRODUCT_LIST_COUNT" resultType="java.lang.Integer">
		SELECT
		COUNT(*)
		FROM product INNER JOIN display_info
		INNER JOIN product_image
		INNER JOIN file_info
		ON product.id = display_info.product_id
		AND
		product.id = product_image.product_id
		AND product_image.file_id =
		file_info.id
		AND product_image.type = 'th'
	</select>

	<select id="SELECT_PRODUCT_LIST_COUNT_BY_CATEGORY"
		resultType="java.lang.Integer">
		SELECT COUNT(*)
		FROM product INNER JOIN display_info
		INNER
		JOIN product_image
		INNER JOIN file_info
		ON product.id =
		display_info.product_id
		AND product.id = product_image.product_id
		AND
		product_image.file_id = file_info.id
		AND product_image.type = 'th'
		AND
		product.category_id = #{id}
	</select>

	<select id="SELECT_PROMOTION_LIST" resultType="PROMOTION">
		SELECT promotion.id AS id,
		product_image.product_id AS product_id,
		file_info.save_file_name AS product_image_url
		FROM promotion INNER JOIN product_image
		INNER JOIN file_info
		ON promotion.product_id = product_image.product_id
		AND product_image.file_id = file_info.id
		AND product_image.type = 'th'
	</select>


</mapper>